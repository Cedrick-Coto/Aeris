namespace Aeris.Engine;

public enum SystemPhase
{
    Initialization = 0,
    Perception = 100,
    Cognition = 200,
    Planning = 300,
    Action = 400,
    Consequences = 500,
    Presentation = 600,
    Shutdown = 700
}
